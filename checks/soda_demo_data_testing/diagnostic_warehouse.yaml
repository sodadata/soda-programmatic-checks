checks for diagnostic_warehouse:

  - schema:
      name: Any schema changes
      warn:
        when schema changes:
          - column delete
          - column add
          - column index change
          - column type change

  - row_count > 0

  - missing_count(primary_key) = 0
  - missing_count(rows_data) = 0
  - missing_count(scan_time) = 0
  - missing_count(dataset) = 0
  - missing_count(check_name) = 0
